<link rel="stylesheet" href="<%= assetPath('css/comment.css') %>">

<li style="list-style: none;" id="comment-<%= comment._id %>">
    <p>
        <% if (locals.user && locals.user.id == comment.user.id){ %>
            <button class="delete-btn no-btn">
            <a class="delete-comment-btn delete-post-btn" href="/comments/destroy/<%= comment.id %>"><i class="fa fa-trash-o" aria-hidden="true"></i> X</a>
            </button>
        <% } %>
        <%= comment.content %>
        <br>
        <small>
            <%= comment.user.name %>
        </small>
        <!-- CHANGE :: display the likes of this comment, if the user is logged in, then show the link to toggle likes, else, just show the count -->
        <!-- study about the data attribute -->
        <small>
            <% if (locals.user){ %>
                <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%=comment._id%>&type=Comment">
                        <%= comment.likes.length %> <i class="fa fa-thumbs-o-up" aria-hidden="true"></i>

                </a>
            <% }else{ %>
                <%= comment.likes.length %> Likes
            <% } %>
        </small>

    </p>    

</li>